steps:
options:
  logging: CLOUD_LOGGING_ONLY
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['beta', 'functions', 'deploy', '${_NAME}', '--runtime=nodejs14', '--trigger-http', '--allow-unauthenticated']
